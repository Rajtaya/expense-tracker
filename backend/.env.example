DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/expense_tracker?schema=public"
JWT_SECRET="generate-a-long-random-string"
JWT_EXPIRES_IN="7d"
PORT=4100
FRONTEND_URL="http://localhost:3000"
# Cloudflare Turnstile secret key (get from dash.cloudflare.com → Turnstile).
# Default below is Cloudflare's test secret (always passes) — replace in production.
TURNSTILE_SECRET_KEY="1x0000000000000000000000000000000AA"
